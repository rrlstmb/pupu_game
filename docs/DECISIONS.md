# Decisions

ADR entries are append-only. Reversible assumptions are acceptable when recorded here.

| ID | Date | Decision | Reason | Cost | Reversible | Status |
|---|---|---|---|---|---|---|
| ADR-0001 | 2026-07-07 | Use Phaser 3 + TypeScript + Vite + Vitest + Playwright + npm. | Empty project; plan recommends Phaser or PixiJS; Phaser gives scene/input/audio/camera primitives and broad examples. | Phaser-specific adapter boundaries must be enforced. | Yes, before runtime code grows. | Accepted |
| ADR-0002 | 2026-07-07 | Phase 00 does not scaffold app code or package files. | User explicitly scoped Phase 00 to audit, decisions, and documents; gameplay and toolchain scaffold belong later. | No executable app yet. | Yes. | Accepted |
| ADR-0003 | 2026-07-07 | Use `src/domain` for pure rules and keep Phaser in scenes/adapters. | Required for deterministic tests and rule/display separation. | More explicit mapping code between domain and Phaser. | Difficult after gameplay grows, easy now. | Accepted |
| ADR-0004 | 2026-07-07 | Use injected seeded RNG for all rule-affecting randomness. | Throws, spawns, wind, and events must be reproducible and fair. | Every system needs RNG plumbing. | No, should remain invariant. | Accepted |
| ADR-0005 | 2026-07-07 | Use data-driven JSON-compatible definitions for levels, NPCs, poop, score, combo, and alert. | The plan requires tunable 10-level content and tactical differences. | Requires schema validation before authoring content at scale. | No, should remain invariant. | Accepted |
| ADR-0006 | 2026-07-07 | Roadmap uses 26 phases and 6 Release Gates. | User acceptance requires this structure; plan provides MVP, content, and release scale. | More phase docs to maintain. | Yes, by review before Phase 01. | Accepted |
| ADR-0007 | 2026-07-07 | Treat missing `AGENTS.md` and `docs` as Phase 00 work, not a blocker. | Phase 00 has no dependency and explicitly requires creating them. | First phase evidence must document absence. | Not needed after Phase 00. | Accepted |
| ADR-0008 | 2026-07-07 | Scaffold with npm, Vite dev server on `127.0.0.1`, strict TypeScript, ESLint, Vitest, and Playwright Chromium. | Provides a stable local browser game baseline and avoids exposing dev server outside localhost by default. | E2E requires Playwright browser download and local port permission in this sandbox. | Yes, before wider tooling grows. | Accepted |
| ADR-0009 | 2026-07-07 | Define world spatial language through a pure `WorldLayout` model and `Depths` constants. | Keeps scene rendering driven by one coordinate model and makes lanes/bounds testable across resolutions. | Scene drawing has an extra data mapping layer. | Yes, before gameplay entities depend on lane data. | Accepted |
| ADR-0010 | 2026-07-07 | Resolve simultaneous left/right input to neutral movement and clear input on blur/visibility/dispose. | Neutral resolution is deterministic and prevents hidden priority; clearing input prevents stuck keys after focus changes. | Players cannot intentionally override a held direction by pressing the other direction without releasing. | Yes, before advanced input customization. | Accepted |
| ADR-0011 | 2026-07-08 | Use canonical pixels, px/s velocity, and px/s^2 gravity/wind for projectile motion. | Matches Phaser rendering coordinates and keeps prediction/simulation directly comparable. | Future camera scaling must convert only at the presentation boundary. | No, should remain stable after golden cases. | Accepted |
| ADR-0012 | 2026-07-08 | Define landing as crossing a configured horizontal landing plane after entering the playable arc. | Supports throws launched from rooftop edge toward alley floor while avoiding immediate landing when origin is below a higher plane. | Sloped or object-specific collisions require later collision layers. | Yes, before collision phase. | Accepted |
| ADR-0013 | 2026-07-08 | NPC spawn overflow skips and counts failed spawns instead of queuing. | Prevents high-density events from creating unbounded backlog or burst allocation. | Spawn cadence can thin out under max-active pressure. | Yes, before authored high-density events. | Accepted |
| ADR-0014 | 2026-07-08 | Phase 06 collision uses projectile segment AABB overlap plus a `${projectileId}:${npcId}:${hitWindowId}` token. | Segment overlap prevents fast projectiles from tunneling through placeholder NPC bodies; hit tokens prevent duplicate collision callbacks from double-counting the same window. | Placeholder rectangular collision is coarse and may need per-poop/per-NPC collider data later. | Yes, before final collider tuning. | Accepted |
| ADR-0015 | 2026-07-08 | Scoring remains unimplemented and is reserved for `NPC_RANT_STARTED`, not `PROJECTILE_HIT`. | The product invariant says rant/reaction is the scoring moment; Phase 06 must not implement formal score early. | Future score systems must consume gameplay events rather than collision callbacks directly. | No, should remain an invariant. | Accepted |
| ADR-0016 | 2026-07-08 | Phase 07 score is calculated only from validated `NPC_RANT_STARTED` events and stored as immutable `ScoreBreakdown` entries. | Keeps UI from awarding points and makes each score source auditable. | Gameplay events need stable ids and scoring metadata. | No, should remain an invariant. | Accepted |
| ADR-0017 | 2026-07-08 | `Hit`, `Ranting`, and `Recovering` pause the combo timer as a hit-stop window. | Lets players read the reaction that caused scoring without losing combo time; prompt required hit-stop behavior to be explicit. | Combo duration feels more forgiving during repeated-hit loops. | Yes, before final pacing tuning. | Accepted |
| ADR-0018 | 2026-07-08 | Alert changes are source-recorded domain events on `AlertState`, not direct numeric mutation. | HUD/debug need to explain why alert changed, and tests need reproducible source accounting. | Per-frame changes require throttling so recent sources remain readable. | No, should remain an invariant. | Accepted |
| ADR-0019 | 2026-07-08 | Retry uses `scene.restart()` plus explicit GameScene state reinitialization in `create()`. | Phaser reuses scene instances; without explicit field reset, caught/score/tokens can leak after retry. | New runtime fields must be added to the reset block. | Yes, if a later level-session owner centralizes state reset. | Accepted |
| ADR-0020 | 2026-07-10 | Model tactical poop as data definitions plus capability strategies. | Phase 09 needs normal, sticky, splash, and jumbo poop to change decisions without embedding rule switches throughout collision code. | Strategy plumbing adds indirection for the small initial set. | Yes, before many more poop types ship. | Accepted |
| ADR-0021 | 2026-07-10 | Sticky refreshes a same-type slow effect, splash dedupes per effect instance, and jumbo only marks `breaksDefense` in Phase 09. | These rules make stacking and repeated hits testable while avoiding future-phase defense implementation. | Sticky cannot stack multiplicatively yet; defense destruction remains deferred. | Yes, before balance lock. | Accepted |
| ADR-0022 | 2026-07-10 | Store projectile config/rules on each active projectile. | Phase 10 has multiple projectile behaviors; active arcs must not change when the player switches arsenal selection. | Projectile state is larger and tests need richer fixtures. | No, should remain stable for deterministic throws. | Accepted |
| ADR-0023 | 2026-07-10 | Stink zones use a reusable `EnvironmentalEffectZone` domain state with lifecycle stats. | Future cleaner systems need a clearing interface, and Phase 10 needs zone lifetime/object leak evidence. | Full pathfinding is deferred; current fallback is slow-in-place rather than route recomputation. | Yes, before cleaner/route phases. | Accepted |
| ADR-0024 | 2026-07-10 | Phase 10 arsenal sandbox is debug-only Alt+number selection. | It lets reviewers exercise eight poop types without changing normal Q/E inventory controls or adding final UI. | Browser e2e must avoid timing assumptions around global projectile cooldown. | Yes, when final arsenal UI arrives. | Accepted |
| ADR-0025 | 2026-07-10 | Model advanced NPC behavior with tags and composed abilities plus an interaction matrix. | Phase 11 adds many NPC types; type switches in scene/collision code would be brittle. | Definitions and fixtures are more verbose. | No, should remain the extension model. | Accepted |
| ADR-0026 | 2026-07-10 | Missing NPC x poop matrix entries use safe default normal behavior and are reported by validation. | The full authored matrix will grow; development should not crash on missing pairs, but gaps must be visible. | Designers must review missing-pair reports before content lock. | Yes, before final matrix lock. | Accepted |
| ADR-0027 | 2026-07-14 | Treat `docs/CODEX_PHASE_PLAN.md` V2.0 as the source for the 26-prompt and Gate A-F dependency map. | Retro Gate A found that the standalone roadmap still described an obsolete phase split. | Roadmap, acceptance, and quality-gate summaries must be kept synchronized with the manual. | Yes, through a reviewed manual revision. | Accepted |
| ADR-0028 | 2026-07-14 | A scene disposer removes both shutdown/destroy hooks when either lifecycle event fires; input also clears on pause and resume. | Phaser reuses Scene instances, so an untriggered counterpart listener and held pause input can survive repeated lifecycle transitions. | Adapters register two additional scene listeners and lifecycle tests must account for Phaser's own baseline listeners. | No, this is a cleanup invariant. | Accepted |
| ADR-0029 | 2026-07-14 | Pool Phaser projectile views separately from pure projectile state. | Prompt 04 required object-pool reuse, while the adapter previously destroyed every recycled view. | The adapter tracks active/pooled/created/reused counts and must destroy both collections on shutdown. | Yes, if replaced by an equivalent bounded renderer pool. | Accepted |
| ADR-0030 | 2026-07-14 | Separate collision, rant start, and recovery into state-bound domain events. | Retro Gate B found collision emitted the scoring event before the NPC actually entered `Ranting`, while `NPC_RECOVERED` was never emitted. | NPC state carries pending immutable rant context and the scene compares consecutive state snapshots. | No, event causality is a core-loop invariant. | Accepted |
| ADR-0031 | 2026-07-14 | Make scoring idempotent by gameplay `eventId` and retire hit tokens with recycled projectiles. | Replayed events and indefinitely retained tokens could respectively duplicate score or grow session memory. | Score lookup and token pruning add small linear work over bounded session collections. | Yes, if replaced by equivalent indexed deduplication. | Accepted |
| ADR-0032 | 2026-07-14 | Pool Phaser NPC views and treat retry as complete Core Loop session reinitialization. | Retro Gate B found NPC views were destroyed on exit and retry preserved alert history plus debug wind/config. | New runtime fields must join the reset contract; pool statistics are exposed for acceptance. | Yes, if a later LevelSession owner provides equivalent cleanup. | Accepted |
| ADR-0033 | 2026-07-14 | Inject alert stage thresholds through `AlertRules`. | Hard-coded boundaries contradicted the data-driven alert requirement and prevented alternate rule fixtures. | Every stage calculation must receive rules explicitly. | No, alert tuning must remain data driven. | Accepted |
| ADR-0034 | 2026-07-14 | Represent each play attempt as a pure `LevelSession` with an immutable settlement snapshot. | Prompt 12 requires one-shot success/failure, auditable results, pause-safe time, and reset tests without reading scattered scene/UI fields. | GameScene must synchronize existing system metrics into the director each frame. | No, session ownership is a campaign invariant. | Accepted |
| ADR-0035 | 2026-07-14 | Level 1 succeeds immediately at its target score; timeout below target and caught alert are failures. | The prompt requires score-target success plus explicit timeout/caught failure and does not require surviving the full 90 seconds after reaching the objective. | Players can finish before 90 seconds; later level data may need an explicit completion mode if designs vary. | Yes, before authoring Level 2. | Accepted |
| ADR-0036 | 2026-07-14 | Use a 3-second data-driven countdown and session ids shaped as `level:seed:attempt-N`; retries keep the seed and increment the attempt. | This provides a visible start state, deterministic RNG reset, and protection from stale retry events. | Session ids are locally deterministic rather than globally unique. | Yes, if persistence later requires a run id. | Accepted |
| ADR-0037 | 2026-07-14 | Pass the validated `LevelDefinition` into `GameScene` and derive spawn configuration through `LevelDirector`. | Gate C found that direct `LEVEL_01` reads in the scene made authored level rules scene-owned and would require code edits for another definition. | Scene entry points must supply a definition; Level 1 remains the safe default for scene restart compatibility. | Yes, before campaign level selection is implemented. | Accepted |
| ADR-0038 | 2026-07-14 | Separate projectile ground projection from its visual parabolic height using one normalized progress value. | The former sprite trajectory was also the collision trajectory, so visual height made the back/top lane unreliable. | Projectile state and debug views carry both positions; bounce/split adapters must preserve projection metadata. | No, collision must remain independent of presentation. | Accepted |
| ADR-0039 | 2026-07-14 | Normal throws charge on Space hold and fire on release along the player's X; charge selects projection target Y, visual apex, and travel duration. Production trajectory helpers are disabled. | Manual review found the diagonal, always-assisted throw did not match the intended horizontal-position plus depth-power control. The existing ground-projection model can express depth without changing collision rules. | Existing e2e interactions must hold and release Space; debug trajectory rendering remains available only behind the default-off layout overlay. | Yes, tuning values remain data driven in `THROW_CHARGE_CONFIG`. | Accepted |
| ADR-0040 | 2026-07-15 | Use one quantized 1%-100% charge power for meter text, meter fill, target Y, visual apex, and travel duration; map 1% exactly to near Y and 100% exactly to far Y. | A zero-width Phaser fill geometry was not reliably visible, and the previous 5% projectile clamp could disagree with the displayed raw percentage. | Throw power changes in visible 1% increments; the 50% target is 1.18px from the geometric midpoint because the exact endpoint interval is 1%-100%. | Yes, near/far and lane tuning remain data driven. | Accepted |
| ADR-0041 | 2026-07-15 | Supersede ADR-0014's moving segment overlap with a final-landing hit window whose horizontal tolerance derives from each NPC's scaled world width and whose target selection is distance/lane/id sorted. | Charged projection makes the landing point the intended impact location; sprite arc overlap was too strict and coupled collision to presentation. | Ordinary projectiles resolve only when landed, and tuning now includes bounded NPC-size multipliers. | Yes, through equivalent deterministic landing rules. | Accepted |
| ADR-0042 | 2026-07-15 | Present charge as a right-anchored vertical bottom-up meter and render one pooled ground-projection shadow per active projectile. | Players need readable power depth and current in-flight projection feedback without restoring predictive trajectory helpers. | Projectile views gain one ellipse and acceptance/debug snapshots; right-side HUD space is reserved by data-driven margins. | Yes, presentation values and shadow styling remain data driven. | Accepted |
| ADR-0043 | 2026-07-15 | Model authored level pressure changes as one-shot timed events in `LevelDefinition`, with `LevelSession` retaining triggered ids and selecting the active spawn profile. | Level 2 needs a deterministic final-20-second rush without Scene-owned level checks or duplicate triggers. | Session snapshots gain event ids, and later overlapping events need an explicit precedence policy; current policy is latest authored triggered event. | Yes, before multiple concurrent event kinds are authored. | Accepted |
| ADR-0044 | 2026-07-15 | Track hits by NPC type in level metrics and evaluate a generic `npc_hit_target` star condition. | The high-speed-target star must be data-driven and auditable without hardcoding joggers into HUD or Scene logic. | Level result metrics carry a small sparse count map. | Yes, if later objective tags replace NPC type lists. | Accepted |
| ADR-0045 | 2026-07-15 | Emit `PROJECTILE_BLOCKED` with interaction-defined feedback and tags, while keeping it outside valid-hit and rant scoring. | Umbrella defense must explain a failed normal throw without violating the invariant that only a legal reaction scores. | Alert and presentation consume an additional typed event; blocked projectiles still recycle normally. | No, explicit non-scoring defense feedback is a gameplay invariant. | Accepted |
| ADR-0046 | 2026-07-15 | Add data-driven weather presentation and interaction-tag star metrics to LevelDefinition/LevelSession. | Level 3 needs rain and a jumbo umbrella-crack objective without Scene-owned level checks. | Visual definitions become more verbose and result metrics gain one sparse tag counter. | Yes, if later schemas generalize weather/objectives. | Accepted |
| ADR-0047 | 2026-07-15 | Resolve splash from the projectile landing point, sort legal candidates by distance/id, cap targets in poop capability data, and retain per-effect NPC tokens. | Level 4 requires predictable bounded crowd hits while excluding immune/exiting NPCs and preventing duplicate scoring. | Splash no longer centers on a possibly offset primary NPC; balance depends on authored radius and cap. | No, deterministic bounded area resolution is a tactical invariant. | Accepted |
| ADR-0048 | 2026-07-15 | Track the maximum legal targets hit by one splash projectile as a LevelSession metric. | The feature star must require one three-person splash rather than three unrelated splash hits. | Result metrics gain one scalar and GameScene groups validated hit events by projectile id. | Yes, if a general per-action objective reducer replaces it. | Accepted |
| ADR-0049 | 2026-07-16 | Resolve authored timed events in independent channels; use highest priority then authored order within a channel, with Prompt 16 events using `replace`. | Wind and spawn pressure must coexist deterministically without the former latest-event implicit overwrite. | `merge` and `exclusive` are schema-reserved but need behavior when a later level authors them. | Yes, before multi-event content expands. | Accepted |
| ADR-0050 | 2026-07-16 | Resolve wind from level remaining time and store the resulting bounded X acceleration in the same projectile trajectory used by visual, shadow, and collision. | Wind must be announced, reproducible, and unable to desynchronize projectile representations or charge-to-target Y. | Active throws retain their launch segment for their short flight; authored segments should avoid sub-flight oscillation. | Yes, if continuous wind transitions later preserve trajectory continuity. | Accepted |
| ADR-0051 | 2026-07-16 | Extend bouncy poop with named bounded surfaces and `lastSurfaceId`, while preserving the legacy sandbox floor fallback. | Level 5 needs visible legal surfaces, one-bounce safety, and no regression to the Phase 10 arsenal sandbox. | Current surface resolution occurs at trajectory completion rather than continuous swept intersection. | Yes, through an equivalent deterministic surface solver. | Accepted |
| ADR-0052 | 2026-07-16 | Treat stink zones as bounded domain instances with oldest-first replacement, per-zone NPC entry tokens, and explicit active/being-cleaned lifecycle. | Level 6 needs deterministic placement, lifetime, cap, scoring dedupe, and cleanup without frame-based rewards. | Overlapping zones apply only the strongest slow while each zone retains its own one-time affected-NPC statistic. | Yes, through equivalent deterministic stacking data. | Accepted |
| ADR-0053 | 2026-07-16 | Coordinate cleaner locks in a pure system using nearest distance, then zone creation order and id; reserve one zone per cleaner and require warning plus cleaning duration before clear. | Multiple cleaners must not instantly or repeatedly clear the same zone, and target selection must reproduce under the same seed. | Cleaners stop in place during the placeholder cleaning state rather than pathfinding toward zones. | Yes, when deterministic approach navigation exists. | Accepted |
| ADR-0054 | 2026-07-16 | Add an independent exclusive `cleanupChannel`; the authored cleaning truck warns, delays, then clears all active zones in creation-order while spawn and presentation channels remain active. | The climax must not overwrite spawn pressure and must reset through the existing LevelSession event-id contract. | `exclusive` currently selects one highest-priority active event through the shared channel resolver; concurrent cleanup merge is deferred. | Yes, before another cleanup event is authored. | Accepted |
| ADR-0055 | 2026-07-16 | Counterattacks lock `playerX` when their telegraph is scheduled, then keep that coordinate through flight and resolve one hit or miss at completion. | The retaliation must be readable and dodgeable without post-launch tracking or hidden hit chance. | Counter projectiles use endpoint collision rather than swept player overlap. | Yes, if a later swept solver preserves snapshot targeting. | Accepted |
| ADR-0056 | 2026-07-16 | Schedule angry-NPC retaliation through a bounded deterministic source-id queue with global gap, per-source exclusivity, concurrency caps, and minimum uncovered rooftop width. | Splash or climax hits must not create synchronized or unavoidable barrages. | Requests can wait or be cancelled when their source exits; queue order favors lower stable NPC ids. | Yes, through an equivalent deterministic fair scheduler. | Accepted |
| ADR-0057 | 2026-07-16 | Reuse `hazardChannel` for the Level 7 climax and apply counter hits as sourced Alert plus temporary speed reduction, throw lock, and invulnerability without health. | Hazard pressure must coexist with spawn/presentation channels and preserve the existing caught failure model. | Combo is not directly removed by a counter hit; the authored penalty focuses on Alert and short control impairment. | Yes, through LevelDefinition tuning. | Accepted |
| ADR-0058 | 2026-07-16 | Model camera pressure in an independent pure `SurveillanceSystem`: authored one-shot snapshot zones and continuous recording exposure share bounded scheduling but have distinct resolution rules. | Level 8 must be readable, deterministic, different from counterattack targeting, and independent of sprite/camera zoom. | A camera source uses one authored rooftop interval at a time; placeholder presentation is intentionally coarse. | Yes, through equivalent authoritative world-zone rules. | Accepted |
| ADR-0059 | 2026-07-16 | Add a priority-selected `surveillanceChannel` that changes camera cadence/capacity without replacing spawn, hazard, cleanup, wind, or presentation channels. | The live-stream climax requires camera and NPC pressure to coexist while preserving cross-channel state. | Same-channel authored events currently use the shared highest-priority/latest-authored resolver. | Yes, before multiple simultaneous surveillance profiles are authored. | Accepted |
| ADR-0060 | 2026-07-16 | Treat camera outcomes as mutually exclusive instance results; cancelled/interrupted cameras do not count as avoided, and capture applies sourced Alert plus short throw lock/invulnerability without health. | Metrics, stars, and penalties must be idempotent and reset-safe, while hitting a camera can interrupt recording at a small data-driven Alert cost. | Captured combo policy remains `keep` in this phase; later tuning can add a data field without changing capture ownership. | Yes. | Accepted |
| ADR-0061 | 2026-07-16 | Model guards and searchlights in an independent pure `SecuritySystem` using authoritative one-dimensional rooftop observation intervals, typed cover occlusion, continuous detection, and threshold-only Alert results. | Level 9 needs moving sight pressure and cover/exposure decisions distinct from Level 8 snapshot/recording cameras, without sprite or zoom-dependent collision. | The horizontal model does not represent vertical ray casting; authored source direction is expressed through intervals and source types. | Yes, through an equivalent canonical-world visibility solver. | Accepted |
| ADR-0062 | 2026-07-16 | Treat throw exposure as one bounded token that temporarily removes cover benefit and multiplies security detection, while leaving charge-to-target Y and projectile physics untouched. | Hiding must not become permanent risk-free output, and exposure must pause/reset with gameplay state. | `coverEffectivenessWhileExposed` is currently authored as zero for Level 9; partial cover effectiveness would require a scalar occlusion result. | Yes, through LevelDefinition tuning and a scalar extension. | Accepted |
| ADR-0063 | 2026-07-16 | Resolve rooftop blockade with pure horizontal interval subtraction, one-unit closed-boundary clearance, deterministic nearest relocation, and minimum movement/cover/throw-space validation; keep `securityChannel` and `blockadeChannel` independent. | Horizontal-only movement makes a blocked-edge placement or channel overwrite a soft-lock risk. | Blockades are static authored intervals after activation; moving barricades are deferred. | Yes, through an equivalent deterministic reachability model. | Accepted |
| ADR-0064 | 2026-07-17 | Model the Level 10 Boss as an atomic token-deduplicated phase machine with ordered interaction gates, not hit points. | The encounter must teach prior mechanics and cannot complete from damage, elapsed time, animation, or score alone. | Scene integration must translate validated camera/projectile interactions into typed gate tokens. | No, no-HP legal-interaction completion is a product rule. | Accepted |
| ADR-0065 | 2026-07-17 | Route every third-phase blockade and combined danger snapshot through `FinalEncounterSafetyCoordinator`, preserving reachable, safe, throw, and Boss-hit intervals. | Individually safe security, surveillance, and counterattack zones can combine into a horizontal soft lock. | The coordinator uses canonical one-dimensional intervals and does not solve arbitrary 2D navigation. | Yes, through an equivalent deterministic shared safety solver. | Accepted |
| ADR-0066 | 2026-07-17 | Keep final golden attempts as encounter-owned inventory granted once on third-phase entry and accept success only from a legal landing token during an active final window. | Earlier golden use must not soft-lock the finale, while animation/time/other poop must never impersonate the final hit. | Final inventory is synchronized into the existing HUD inventory but remains session- and encounter-scoped. | No, legal final collision is the completion invariant. | Accepted |
| ADR-0067 | 2026-07-17 | Reserve `bossChannel` for phase/protection/window/result commands; it has exclusive priority within its channel and never replaces spawn, security, surveillance, blockade, cleanup, hazard, wind, or presentation state. | Boss transitions need highest orchestration priority without hidden cross-channel mutation. | Existing adapters still coordinate explicit profile changes in `GameScene`; a general command bus remains deferred. | Yes, before later campaign/persistence orchestration. | Accepted |
| ADR-0068 | 2026-07-17 | Use one ordered `CAMPAIGN_LEVELS` registry as the source for menu entries, next-level routing, and the terminal Campaign result. | Gate D found duplicate hand-authored menu buttons and a nested HUD route chain that left Level 10 on a generic unavailable-next placeholder. | Menu positions remain authored presentation metadata, but level order and terminal behavior now have one tested source. | Yes, if a later campaign service preserves the same registry contract. | Accepted |
| ADR-0069 | 2026-07-17 | Normalize keyboard and mouse adapters into one `GameplayInputIntent`, with first-press charge ownership and held-keyboard movement priority. | Mouse play must reuse movement and throw rules, avoid duplicate projectiles under mixed input, and remain independent of canvas scale. | Native pointer listeners require explicit capture, UI routing, blur/pause/reset cleanup, and listener diagnostics. | Yes, if a later control-remapping layer preserves the same intent and ownership contract. | Accepted |
| ADR-0070 | 2026-07-20 | Register all Phase 22 runtime presentation assets in an audited manifest with explicit status, source, and fallback; reject unknown-source runtime assets. | The vertical slice needs replaceable visuals without introducing rights ambiguity or scattered Scene paths. | Generated prototypes are deliberately not final art and require later replacement. | Yes, while preserving manifest/source policy. | Accepted |
| ADR-0071 | 2026-07-20 | Keep authoritative gameplay bounds separate from skin scale/pivot and map domain state one-way into character animation state. | Sprite size/frame/animation must never affect hit, lane, camera, hazard, or Boss rules. | Presentation adapters contain explicit mapping/reset code. | No, this boundary is a gameplay invariant. | Accepted |
| ADR-0072 | 2026-07-20 | Deduplicate primary VFX and audio by gameplay token and cap presentation pools/voices. | Splash/crowd/Boss events otherwise create duplicate feedback and unbounded display/audio allocation. | Secondary ambience is intentionally conservative in this prototype. | Yes, through equivalent bounded ownership. | Accepted |
| ADR-0073 | 2026-07-20 | Use semantic audio events backed by repository-generated WebAudio cues with silent fallback. | Scene code should not know asset keys, rights-unknown audio is forbidden, and autoplay/load failures must not affect gameplay. | Music and effects remain prototype synthesis rather than production recordings. | Yes, by replacing manifest definitions. | Accepted |
| ADR-0074 | 2026-07-20 | Treat Opening and Level Intro as presentation-owned, skippable overlays that never emit gameplay completion or throw events. | Skip clicks/pointerup must not produce ghost throws or duplicate Scene transitions. | Opening is explicitly selected from Menu in Phase 22 rather than forced every session. | Yes, if a future session director preserves the latch/input cleanup contract. | Accepted |
| ADR-0075 | 2026-07-20 | Avoid coordinate-transforming gameplay camera effects in Phase 22; use bounded overlays/rings so canonical mouse mapping remains unchanged. | Existing mouse world mapping is canonical canvas based and must remain stable under polish. | Dramatic pan/zoom remains available later after a camera-aware mapper is reviewed. | Yes. | Accepted |
| ADR-0076 | 2026-07-20 | Reuse bounded NPC/projectile pools and add a fixed 24-view feedback pool with full visual reset on release/shutdown. | Retry/menu soak must not accumulate views, tweens, trails, text, or stale skins. | Pool capacities can be tuned from measured Prompt 25 performance. | Yes. | Accepted |
| ADR-0077 | 2026-07-21 | Store one runtime-validated versioned SaveData document behind `SaveRepository`, with one same-origin backup document. | Scenes must not trust JSON or call localStorage, and an interrupted replacement must not destroy the only known-good save. | Backup is local best effort, not transactional filesystem durability. | Yes, through a repository preserving future-version safety. | Accepted |
| ADR-0078 | 2026-07-21 | Keep permanent progression strictly separate from LevelSession and encounter state. | Inventory, Boss/final-golden, hazards, charge, input, and event channels must always initialize from authored runtime definitions. | Continue resumes a level choice, never an in-progress run. | No. | Accepted |
| ADR-0079 | 2026-07-21 | Commit one settled result as one monotonic transaction and retain a bounded 256-token receipt set. | Duplicate success/ResultScene delivery must not duplicate completion count, unlocks, records, or presentation. | Very old receipts can age out; each new run uses a fresh run ID. | Yes, with an equivalent idempotency policy. | Accepted |
| ADR-0080 | 2026-07-21 | Never overwrite a future schema automatically; use temporary memory progress until explicit Reset. | Downgraded clients cannot safely interpret or rewrite newer user data. | Progress made in the incompatible session is intentionally nonpersistent. | No. | Accepted |
| ADR-0081 | 2026-07-21 | Use `RunContext` and `progressionEligibility` to isolate Campaign, Free Play, and challenge records. | Reusing LevelDefinition must not let alternate modes unlock Campaign content or alter Campaign bests. | Mode launch assembly carries explicit context into every retry. | Yes. | Accepted |
| ADR-0082 | 2026-07-21 | Allow challenge overrides only for duration, target, poop availability/stock, seed, and bounded spawn interval scaling. | Extra modes need recognizable rules without changing physics, hit invariants, final Boss legality, or safety coordinators. | More mode variety requires extending and testing the allowlist. | Yes. | Accepted |
| ADR-0083 | 2026-07-21 | Derive Daily Mission seed from the device local calendar date formatted `YYYY-MM-DD`. | The phase plan permits local deterministic daily content without a backend. | Devices in different time zones change challenge at their own local midnight. | Yes, only with an explicit migration/product change. | Accepted |
| ADR-0084 | 2026-07-21 | Persist only local, non-personal progression and send no save data over the network. | Prompt 23 has no account/backend need and must not introduce tracking or personal-data risk. | There is no cross-device sync or server leaderboard. | No. | Accepted |

## Unknowns and Reversible Defaults

- Phaser, Vite, Vitest, Playwright, TypeScript, and ESLint versions are now locked by `package-lock.json`.
- npm script names are finalized for Phase 01 in `package.json`.
- CI provider is unknown; Phase 01 should keep scripts provider-agnostic.
- Asset pipeline is unknown; use placeholder assets only when validating presentation, never rule completion.
- Browser support targets are unknown; default to current Chromium for Playwright smoke until product targets are specified.
